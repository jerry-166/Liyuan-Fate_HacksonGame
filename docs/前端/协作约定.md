# 前端协作约定

> 本文档记录前端与后端（人 B）、内容/美术（人 C）之间的协作交叉点。

---

## 与后端（人 B）的协作点

| # | 前端当前状态 | 对人 B 意味着什么 | 优先级 |
|---|------------|-----------------|--------|
| 1 | `UIScene` 已完整实现（对话/SSE/选项/自由输入/阶段过渡/结局/历史面板/章节系统/物品展示/存档槽位） | B 的 SSE 流式结构和 API 响应字段已对接完成 | 🟢 P2 |
| 2 | `api/client.js` 16 个 API 函数全量接入（v2 章节驱动版，含 Mock + 真实双模式） | B 确保真实 API 与 Mock 数据结构完全对齐 | 🟡 P1 |
| 3 | 六章节色调 + `CHAPTER_MAP` 已定义，`tintOverlay` 自动应用 | B 在 SSE `done` 事件中准确返回 `current_chapter` + `chapter_completed` | 🟡 P1 |
| 4 | 对话历史存储在后端 SQLite | B 的状态管理器支持 `GET /api/game/{id}` 返回完整状态 | 🟡 P1 |
| 5 | NPC 气泡文本从 API 状态 `current_greeting` 动态更新（`refreshNPCsFromState`） | B 的 `POST /api/game/start` 及状态接口返回准确 `current_greeting` | 🟢 P2 |
| 6 | SSE data 行解析依赖格式一致性 | B 确保 SSE data 行是单行紧凑 JSON | 🟡 P1 |
| 7 | 自由文本输入框为 DOM `<input>`（`createGlobalInput()` 工厂） | B 的 `POST /api/dialogue` 支持 `player_message` 字段即可复用 | 🟢 P2 |
| 8 | 章节系统：`startChapter`/`getChapter`/`getTask` 已接入 | B 的章节 API 返回 `task.sub_tasks` + `npc_completion_votes` 结构 | 🟡 P1 |
| 9 | 物品展示对话：`showItemToNpcStream` + `getItems` 已接入 | B 的 `/api/dialogue/show-item` 需支持物品上下文对话 | 🟢 P2 |
| 10 | 本地存档槽位：数字键 1-6 存档，Ctrl+数字键读档 | B 的 `GET /api/game/{id}` 需支持从槽位恢复完整状态 | 🟢 P2 |

## 与内容/美术（人 C）的协作点

| # | 前端当前状态 | 对人 C 意味着什么 | 优先级 |
|---|------------|-----------------|--------|
| 1 | 所有角色和地图是程序化绘制，无外部图片 | C 提供真实的 tile 贴图 + NPC sprites | 🔴 P0 |
| 2 | NPC 从 API 状态动态创建，本地配置 5 种（陈师傅、小华、老周、梅姨、船夫老李） | C 的人设卡直接影响 NPC 外观设计（5 种配色方案） | 🟡 P1 |
| 3 | NPC 初始问候语从 API `current_greeting` 动态获取 | C 提供更丰富的对话库 | 🟢 P2 |
| 4 | 地图布局与 C 的"关键地点"对齐 | C 基于此布局设计更精细的 tile 贴图 | 🟡 P1 |
| 5 | 六章节色调定义（melancholy→cold→warm→dramatic→somber→dramatic）与 C 的叙事六章节对齐 | C 提供色调指导（和可能的背景替换图） | 🟢 P2 |
| 6 | 对话选项目前只有 Mock | C 设计关键剧情节点和结局 | 🟡 P1 |
| 7 | 音箱/音乐未实现 | C 的 AI 音乐产出需与阶段色调一一对应 | 🟢 P2 |

## 综合协作状态

```
前端 (人A)          后端 (人B)            内容 (人C)
──────────────────────────────────────────────────
✅ 地图渲染          🔴 后端代码进行中    🔴 NPC人设卡未交付
✅ WASD控制          🟡 API 已设计        🔴 tile/角色图未交付
✅ UIScene完整实现   ✅ Mock 数据已提供    🟡 微剧本大纲进行中
✅ 16个API全量接入   🟡 架构方案完成       🟡 音乐/音效未启动
✅ 存档管理系统
✅ 自由文本输入
✅ v2章节系统
✅ v2物品系统
✅ 本地存档槽位
✅ 5 NPC动态创建
```

> **MVP 关键路径**：A 实现 UIScene(对话UI+章节+物品) ← 依赖 → B 完成后端核心链路(含章节/物品 API)、C 提供 NPC 人设(5 种) + 基础美术。三者需要同步推进。
